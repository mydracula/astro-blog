---
import Common from '@components/Common.astro'
import Main from './Main.astro'
const { page, post, frontmatter } = Astro.props
---

<Common page={page} frontmatter={frontmatter}>
  <Main post={post} />
</Common>
